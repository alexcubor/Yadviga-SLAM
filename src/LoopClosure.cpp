// Checks if the user has returned to an already explored area (loop closure detection).
// Compares current keypoints with previously saved ones (for example, through Bag of Words).
// Corrects the map and camera position if a loop is detected.